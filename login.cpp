#include <iostream>
using namespace std; 

void login(string car_plate)
{
    bool is_in_database; 
    bool there_is_fine; 
    float fine; 
    //check if the car_plate is in the database 
    if(is_in_database )
    {
          cout<<"Your carbon emission level is: "<<endl ;
        if(there_is_fine)
        {
            // get fine amount from the database tagged to the car plate and display it on the user account. 
            cout<<"The fine is"<<fine <<endl;
        }
        
    }
    
    

}